from building import *
import rtconfig

# get current directory
cwd     = GetCurrentDir()
# The set of source files associated with this SConscript file.
src     = Glob('*.cpp')
path    = [cwd]

group = DefineGroup('CFF/gyro', src, depend = ['PKG_USING_CFF_IMU'], CPPPATH = path)

Return('group')
