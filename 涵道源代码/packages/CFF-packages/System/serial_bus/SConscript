from building import *

cwd = GetCurrentDir()

src     = Glob('src/*.c')
path    = [cwd + '/inc']

if GetDepend('RT_USING_CPLUSPLUS'):
    src    += Glob('src/*.cpp')
 
group = DefineGroup('CFF/System', src, depend = ['PKG_USING_CFF_SERIAL_BUS'], CPPPATH = path)

Return('group')